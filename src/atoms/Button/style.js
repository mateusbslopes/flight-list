const style = (bgColor, borderColor) => `
    background-color: ${bgColor};
    border-color: ${borderColor || "white"};
    border-radius: 9px;
    border-width: 1px;
    outline: inherit;
`;

export default style;
